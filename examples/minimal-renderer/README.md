# Minimal renderer consumer

This application proves that a separate product can import a ZIP, apply typed
field values, render with the same backend decisions as Template Studio, expose
the product-render identity, and export PNG without adopting Studio UI.

```sh
pnpm example:dev
```
