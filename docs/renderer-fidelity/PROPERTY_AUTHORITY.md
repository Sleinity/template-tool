# Property-Authority Inventory

## Renderer rollout authority

Renderer mode is an internal operational preference, not an imported property. Raw and canonical authority are absent; `PersistedRendererRolloutPreferenceV1` owns the requested global mode, `ResolvedRendererRolloutDecisionV1` owns effective mode and selected visible owners, and family contracts remain semantic authority for capability eligibility. Missing or corrupt preference state safely defers to the existing surface default. Export consumes the same rollout decision and persistence identity as the visible renderer.

## Backend and diagnostic authority

| Property | Raw/canonical authority | Live-resolution authority | Surface/export authority | Fallback | Confidence |
| --- | --- | --- | --- | --- | --- |
| Backend selection | Canonical scene plus existing resolved family capability records | `ResolvedBackendDecisionV1` selects the owner chain and binds source/resolved revisions | Every renderer surface and hidden PNG consumes the same decision IDs | Explicit compatibility or unsupported-preservation owner | High for selection; individual feature confidence remains family-specific |
| Backend availability | Roadmap and ADR evidence | `ResolvedBackendAvailabilityV1` once per tree | Diagnostic/telemetry only | Canvas/offscreen and WebGL unavailable | High |
| Capability diagnostic grouping | Existing source/resolved warnings and fallbacks | `ResolvedBackendDiagnosticProjectionV1` | Quality report and Import Inspector presentation | Technical trace when not user-actionable | High for projection; visual impact remains evidence-dependent |

“Current” records existing pixel behavior. Milestone 4 migrates a bounded core layout/text family to explicit runtime ownership while retaining coherent compatibility routing. Enrichment remains lower priority than explicit canonical/user data.

For routed core subtrees, `CoreLayoutSettlementV1` owns final x/y/width/height, supported Auto Layout, parent HUG, sibling FILL, image-slot, rectangular clip, and export-root geometry on every live surface. The browser owns only revision-tagged intrinsic text metrics. Fields and Validate retain static compatibility appearance while applying eligible settled core geometry; masks, transforms, complex constraints, circular FILL-in-HUG, and advanced visual families remain compatibility-owned. See [runtime routing](RUNTIME_ROUTING.md), [surface convergence](SURFACE_CONVERGENCE.md), and ADRs 0025–0030.

Milestone 5 appearance contracts are observational projections of canonical candidates and provenance, not new live owners. Masked sibling ranges, offscreen compositing, and backend selection remain unresolved rather than inferred.

Milestone 6 makes `ResolvedImagePlacementIntentV1` the current runtime authority for image fit/crop interpretation. Routed settlement or coherent compatibility still owns the slot; the pure placement resolver owns source/destination geometry; the browser owns only native sampling. See [image placement](IMAGE_PLACEMENT.md) and ADR 0047.

Milestone 7 adds a narrow source-certified mask authority. Strict `node.mask.isMask=true` and package `maskRelationships` own classification and sibling scope; the current resolved mask revision owns exact opaque-rectangular ALPHA lowering; the source fill is semantic `mask-input`, not ordinary RGB output. Unsupported mask types, paints, transforms, geometry, or relations remain explicit compatibility states. See [mask authority](MASK_SOURCE_CONTRACT.md) and ADRs 0050–0052.

Milestone 7.1 adds `PrimitiveAppearanceV1` as the sole runtime visual owner for its capability-selected rectangular subset. Canonical scene properties own semantic intent; current settled bounds own effective radius/stroke geometry; resolved source/geometry/paint/stroke revisions gate publication. Unsupported primitive properties select coherent compatibility instead of split output. See [primitive authority](PRIMITIVE_PAINTS.md) and ADRs 0053–0056.

Milestone 7.2 transfers independent rectangular corners and opaque uniform `CENTER`/`OUTSIDE` stroke geometry to the same bounded primitive owner after receipt of the exact source fixture. Canonical values own intent; current settled bounds own edge-local effective radii and stroke-path geometry; backend selection is singular and capability-based. See [the closed fixture gate](PRIMITIVE_STROKE_FIXTURE_GATE.md), [stroke geometry contract](PRIMITIVE_STROKE_GEOMETRY.md), and ADRs 0057–0061.

Milestone 7.3A transfers only the closed source-certified linear-gradient subset. Same-index `extensions.figma.rawFills` hydrates strict canonical stops/transform with provenance and conflicts; `ResolvedLinearGradientGeometryV1` owns one-inverse normalized geometry and current-bounds SVG geometry; one SVG primitive owns pixels. Stops, stop alpha, paint opacity, node opacity, and source-over remain separate authorities. See [runtime authority](LINEAR_GRADIENT_RUNTIME_AUTHORITY.md) and Accepted ADR 0063.

Milestone 2 now defines the observational scene-contract precedence in `src/template-package/scene/propertyAuthority.ts` (35 stable entries) and [ADR 0015](decisions/0015-property-authority-contract.md). The table below remains the factual current-runtime inventory. Runtime helpers have not migrated, so “live-resolution authority” still describes current pixels rather than the new scene. Every important scene property records the selected candidate, competing values, provenance, confidence, conflict, ambiguity, and fallback.

Scene precedence is: validated `workingPackage`/explicit canonical value; then preserved raw Figma value only when canonical semantics are absent; then optional enrichment only for absence; then explicit safe fallback. User edits are authoritative because they are validated mutations in `workingPackage`. Browser measurements remain separate future settlement inputs.

| Property | Raw source authority | Canonical semantic authority | Live-resolution authority | Override / enrichment precedence | Export authority | Safe fallback | Confidence / ambiguity |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Text characters | Exported node text | `TemplateNode.text` | `ResolvedRenderText.characters`, with raw node fallback | User field mutation wins; empty text restores declared field default; enrichment does not replace text | Hidden editor DOM | Preserve imported text; diagnose missing target | High; imported node vs field default disagreement unresolved |
| Imported text defaults | Node text and editable-field default | `basePackage`, field `defaultValue` | `workingPackage` after restore | Clear restores field default in text path | Current working package | Restore base package | Medium; two default sources |
| User text overrides | User input | Mutated `workingPackage` node | Recreated resolved tree/browser | User wins until cleared | Current editor DOM | Imported/default value | High |
| Text-box width | Exported bounds/sizing | Node bounds and horizontal sizing | Resolved layout plus CSS/flex/constraints | User text does not directly change width; raw Figma may supplement | DOM width at capture | Exported bounds | Medium; static/editor differ |
| HUG text height | Exported bounds, HUG intent, and `text.leadingTrim` | Vertical sizing HUG plus explicit normal/CAP_HEIGHT semantics | Routed: revision-tagged intrinsic metrics and semantic trim box → `CoreLayoutSettlementV1`; compatibility: existing DOM | User text/font/width triggers a new revision; newer exact metrics reject prior fallback work | Routed hidden renderer settlement | Coherent compatibility subtree; never guess trim | High for normal and now-hiring CAP_HEIGHT; advanced text unresolved |
| Fixed text height | Exported bounds/value | Vertical FIXED sizing | Renderer fixed CSS height | User text cannot resize box unless rules change package | DOM height | Exported height | High |
| Font request and face identity | Exported family/style label/weight/PostScript plus raw provenance | `CanonicalFontRequestV1`; linked `CanonicalFontFaceV1` remains separate | Shared semantic matcher selects exact/compatible/replacement/missing; resolution binds request, face, hash and history | Explicit user replacement wins only after confirmation; enrichment cannot replace an explicit source request | Persisted working-package resolution and managed record | Preserve request; explicit missing/replacement | High for now-hiring real upload; source-preview release/features open |
| Font metrics | Exported family/weight/style/PostScript and managed assets | Font requirements/resolution | Hash-derived private loaded family plus Canvas ascent/descent/cap/glyph metrics and DOM baseline calibration | Managed exact face wins; confirmed replacement may provide temporary authority; revision prevents stale overwrite | Private `FontFace`, hash/face identity, prepared `document.fonts`, current measurement revision | Explicit compatibility ownership | High for exact Geist Mono 500 and Inter Tight 700 in Chromium; cross-platform open |
| Vertical text trim | `text.leadingTrim`, raw `extensions.figma.leadingTrim` | `CanonicalSceneGraphV1.text.leadingTrim` with provenance | `CAP_HEIGHT` → cap-to-final-baseline intrinsic box and exact-metric paint origin; `NONE` → existing line box | Canonical beats matching raw evidence; edits retain the same source mode; font revision owns current translation | Current settled trim box and paint origin in hidden renderer | Unknown/missing metrics select compatibility | High for now-hiring; other trim modes unsupported |
| CAP_HEIGHT glyph origin | Exact face, baseline/cap metrics and line height | Trim semantic plus exact font identity | Semantic wrapper owns height; inner browser line box translates by negative first-cap-top offset | HUG bypasses vertical alignment; fixed boxes align the wrapper, not glyph layer | Current hidden renderer paint span | Coherent compatibility; never centre heuristically | High for now-hiring Chromium; cross-platform open |
| Text inspection bounds | Source text box/trim mode | Semantic trimmed box | Shared inspection preview consumes explicit trim telemetry | Current exact/replacement measurement wins by revision | Overlay only; never exported | Rendered compatibility box | High for CAP_HEIGHT live surfaces |
| Container width | Exported bounds/layout | Sizing and canvas | CSS flex/constraint/browser | Canonical edit wins; raw Figma compatibility may supplement | DOM width | Exported bounds | Medium |
| Container height | Exported bounds/layout | Sizing and canvas | CSS flex/HUG/browser | Descendant edits affect HUG ancestors implicitly | DOM height | Exported bounds | Medium; invalidation implicit |
| FILL remaining space | Raw grow/align and layout | FILL sizing | Routed: settlement arithmetic; compatibility: browser flex | Canonical FILL preferred; circular FILL-in-HUG stays compatibility-owned | Routed settlement or compatibility DOM | Coherent compatibility subtree | High for now-hiring dependency chain |
| Image fit mode | Node/paint/asset/hint | Immutable source mode plus optional `image.activePlacement` | `imported-source` uses source CROP/FIT/FILL; replacement Fill/Fit selects cover/contain | Revisioned active replacement wins; otherwise node > paint > hint > asset > default | Same state/revision and intent in hidden DOM | Aspect-preserving centered cover | High for source CROP/FIT/FILL and replacement Fill/Fit; TILE partial |
| Image crop | `imageTransform` plus source scale mode | Canonical image metadata preserves both; active replacement stored separately | Active only for imported-source CROP; normalized slot-to-source matrix is inverted once; every FILL/FIT/replacement preserves it as inapplicable | Replacement Fill/Fit never reuse source crop; future editor-crop owns separate data | Same resolved intent plus current slot in hidden DOM | Diagnosed cover for malformed CROP | High for static real-ZIP CROP and replacement/reset matrix; source resize intentionally deferred |
| Image focal position | Explicit object position; source transform only for CROP | Canonical position/transform | Explicit focal owns FILL/FIT alignment; CROP matrix owns its source polygon; otherwise center | Explicit node/field position > center; transform is not converted into FILL focal | Same placement in hidden DOM | Center | High for centered now-hiring FILL; edit UI coverage partial |
| Image replacement mode | Editor active choice | `image.activePlacement.state/revision`; imported mode remains separate | Upload defaults to replacement-fill; user may switch Fill/Fit | Current monotonic active revision wins; stale decode rejected | Persisted working package and hidden PNG | Deterministic Fill for legacy replacement | High: all three real fields, browser upload, switching, reset/reload and stale work verified |
| Mask source and scope | Exported `node.mask` plus `maskRelationships` | Canonical source record, declared affected range, provenance, capability and revision | Exact opaque rectangular ALPHA subset lowers once to affected-node CSS inset clip; other masks compatibility-owned | No user override; enrichment cannot replace declarations | Same relationship/revision/clip in hidden PNG | Preserve source and explicit unmasked fallback; never infer scope | High for crop-3 subset; other masks unresolved |
| Mask-source paint | Source fill order/color/alpha/opacity/transform | Fill plus `mask-input` role and paint revision | Mask evaluator/exact lowering is sole visual owner; ordinary paint is suppressed | No override path | Same semantic input and owner in hidden PNG | Preserve raw paint; never make transparent or paint twice | High for crop-3 opaque SOLID |
| Ordinary solid paint | Figma RGB plus paint opacity; ordered source fill/index/visibility | Canonical fill plus strict `solid-paint-source-v1` provenance for affected aliases/raw evidence | `PrimitiveAppearanceV1` owns one opaque SOLID or an eligible ordered SOLID stack on rectangles/frames | Working-package changes issue new paint/stack/tree revisions | Same primitive owner/revision in hidden PNG | Coherent compatibility; preserve complete stack | High for single and bounded multiple-SOLID routes |
| Ordered fill stack | Exported `appearance.fills[]` order plus same-index `extensions.figma.rawFills[]` when present | Canonical array preserves every entry/index; `ResolvedOrderedSolidStackV1` retains certified SOLID-only stacks and `ResolvedOrderedNormalPaintStackV1` retains the certified SOLID-then-linear stack | One SVG group/shared clip for eligible NORMAL stacks; source index 0 is backmost; every other combination remains one coherent compatibility owner | Working-package order/value changes recompute; no generic stack edit UI | Same source/geometry/stack/backend identity across all live surfaces and PNG | Preserve complete primitive and exact fallback reason | High for six SOLID-only fixtures and the exact two-layer SOLID-then-linear fixture; IMAGE, reversed, longer, and other mixed stacks deferred |
| Per-paint visibility and opacity | Figma paint `visible`/`opacity`; gradient stop alpha; serialized compatibility alpha evidence | `solid-paint-source-v1` applies affected equal aliases once and preserves original values; differing evidence remains ambiguous | Single-SOLID CSS, ordered-SOLID SVG, and isolated gradient SVG apply their certified values; IMAGE opacity has no stack owner | No generic paint-stack override | Current singular owner for supported subset | Preserve values; whole-stack compatibility when unsupported | High for SOLID/gradient subsets; mixed/image runtime deferred |
| Paint blend mode | Paint `blendMode`; node/group blend and parent isolation remain separate | Per-paint canonical value plus raw provenance | Only NORMAL in singular owners; non-NORMAL has no resolved stack/compositing authority | No edit path; enrichment cannot override source blend | Browser default/compatibility, not source-certified | Preserve and diagnose | High for source occurrence; DARKEN occurs once, other requested modes absent; Phase 9 blocked |
| Primitive bounds and corners | Canonical geometry, transform, ordered corner values | Canonical scene geometry/provenance | Current settled bounds plus primitive geometry revision own uniform and independent edge-local Figma clamping | Current scene/settlement wins; DOM measurement never owns template geometry | Same effective radii/scales/revision and path geometry | Compatibility for non-axis-aligned or unsupported complete primitive | High for stroke-test plus M7.1 uniform controls; smoothing deferred |
| Canvas/root/frame background | Canonical canvas background and ordinary node fills | Separate canvas and node semantic records | `PrimitiveCanvasAuthorityV1` for canvas; eligible primitive paint owner for root/frame | Application and inspection stages never override template pixels | Same separation in PNG | Transparent canonical fallback | High for bb/main regional evidence |
| Root bounds | Canvas and root bounds | Package canvas plus root node | Routed root uses canonical canvas dimensions | Package canvas and explicit root mutation regenerate scene/revision | Settled root plus capture dimensions | Canvas dimensions | High for matched canvas/root; mismatch policy remains |
| Constraints | Raw constraints and bounds | Partial sizing/positioning | Editor constraint helper + transform safety | Raw extensions currently supplement canonical semantics | Editor-mode DOM | Snapshot-and-clip | Medium; Milestone 2 decision |
| Linear gradients | `extensions.figma.rawFills[sourceIndex]` preserves full stops/transform | Strict canonical `gradientStops`, `gradientTransform`, and `linearGradientSource` pair by source index; raw evidence remains preserved | `ResolvedLinearGradientGeometryV1`: raw matrix, determinant, one inverse, handles, stops/opacity, live-bounds SVG transform and revisions | No edit path; canonical values win when consistent, conflict blocks route; enrichment never overrides runtime | One SVG definition/path; same identity in Validate, Fields, editor and PNG | Whole-primitive compatibility with exact reason | High for isolated two/three-stop NORMAL rectangular subset; other families/stacks deferred |
| Strokes | Ordered source paint/index/weight/alignment/visibility/alpha | Canonical stroke stack | Exact eligible rectangular INSIDE/CENTER/OUTSIDE use explicit source/fill/inner/centre/outer/visual bounds; capability selects singular CSS or SVG | Unsupported alignment/style/layout inclusion/self-clip keeps complete primitive compatibility-owned | Same geometry/revisions in live surfaces and hidden PNG | Existing compatibility with explicit reason | High for stroke-test and M7.1 INSIDE controls; advanced strokes deferred |
| Advanced stroke order/style | Raw stroke array, possible dash/cap/join/miter/per-edge extension fields | Canonical order and observational `StrokeStackV1`; no strict per-edge/dash contract | Compatibility resolves visible SOLID layers only; primitive authority accepts one; non-solid and advanced data do not own pixels | No edit path | Compatibility DOM; no source-certified export authority | Preserve raw evidence and diagnose | High absence finding in 17 ZIPs; blocked by Phase 8 geometry or missing source authority |
| Effects | Raw effect list | Canonical effects | Resolved CSS effects | Canonical list; enrichment only adds metadata | CSS effects in DOM | Preserve unsupported effect and diagnose | Medium |
| Blend modes | Raw paint/layer blend | Incomplete/extension-preserved | No explicit resolved authority found | Unresolved | Browser default/no effect | Preserve and diagnose | Low; Milestone 2/6 |
| Variables | Raw collections/aliases/modes | Tokens/extensions/literal resolved values | No live variable evaluator found | Unresolved; user literal edits win in supported fields | Literal DOM values | Preserve raw and resolved literal | Low; Milestone 2/7 |
| Component overrides | Raw instance/component metadata | Generic node hierarchy/extensions | No component override engine found | Exporter-flattened values effectively win | Generic DOM | Preserve source; render flattened children | Low; Milestone 2/7 |
| Export geometry and media raster readiness | Source bounds/layout plus current asset identity | Current `workingPackage`, resolved backend/media placement, and current settlement | Routed properties: current stable settlement; compatibility properties: hidden editor DOM; CSS media additionally requires current decode evidence and one revision-bound browser-raster readiness pass | User edits and current font/asset/placement/backend/settlement revision win; object URL is telemetry only | Hidden renderer with settlement plus ADR 0069 raster-readiness gate | Reject stale routed export or use declared compatibility path; never use a fixed sleep | High for routed geometry and headed/headless CSS-media PNG repeatability |

## Decisions retained as unresolved for later milestones

- Whether editable-field defaults or imported node values are the normative imported default when they disagree.
- Raw Figma promotion into the scene is now explicit, but removal from current runtime helpers remains gated by [the migration map](SCENE_MIGRATION.md).
- Browser measurements are runtime intrinsic inputs for the Milestone 4 core route; final DOM bounds remain compatibility evidence only.
- Whether canvas or root bounds win on mismatch and how that is diagnosed.
- Source FILL/FIT and affine CROP plus editor replacement Fill/Fit are migrated. Fixed FILL no longer consumes CROP-only matrices. Interactive editor crop, TILE, adjustments, and true masks remain unresolved.
- Mask range/bounds semantics, compositing groups, variables, component overrides, and style identity.
- How one settled instance is shared across static inspection and live/export surfaces remains Proposed ADR 0010. Editor and hidden PNG share a deterministic revision/identity; Fields/Validate intentionally remain bounds-first compare surfaces.
