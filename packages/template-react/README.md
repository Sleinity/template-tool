# `@sleinity/template-react`

Private UI-free React integration exposing the proven `TemplatePackageRenderer`
and a small runtime context. It deliberately excludes Template Studio routes,
settings, Validate, Fields, and editor controls.
